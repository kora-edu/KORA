1. The eigenvalues and eigenvectors of A = 3, -2, -1, 2 are λ1 = 1, v1 = 1, 1, λ2 = 4, v2 = 2, -1. Obtain the unique solution to the system of ordinary differential equations ˙y1 = dy1/dt = 3y1 −2y2, ˙y2 = dy2/dt = −y1 + 2y subject to y1(0) = 1, y2(0) = 0. Describe the behaviour of the solution as t → ∞.

2. Consider the ellipse 3x² + 2xy + 3y² = 10 which can be written as [x y] * A * [x y] = 10 where A = [3 1; 1 3]. A has eigenvalues and eigenvectors λ1 = 2, v1 = -1, 1, λ2 = 4, v2 = 1, 1. Find matrices P and D so that A = PD P T, where D is a diagonal matrix. Define new coordinates (x, y) so that the equation of the ellipse is of the form ax² + by² = 10, for real numbers a and b.

3. Evaluate the following integral by reversing the order of ∫ from 0 to 3 ∫ from 2y to 2 (1 + x²) dx dy.

4. Consider using double integration to find the centroid of the triangle with corners at (0, 0), (1, 2), (4, 2).

5. Evaluate the triple integral ∫ ∫ ∫R x² y dV, where R is defined by 0 < x < 1, 0 < y < 2, and 0 < z < 1 - x.

6. Use integration to find the volume of the shape defined by − π/2 < θ < π/2, 0 < r < cos(θ), and 0 < z < r.

7. Find the Laplace transform of ƒ(t) = 2 cosh (2 t) + 3 e^-t.

8. Find the inverse Laplace transform of G(s) = 2 / (s² - 4s + 13) + 1 / s³.

9. Use Laplace transforms to solve dy/dt + 2y = 0, with initial condition y(0) = 1.

10. Use Laplace transforms to solve d²y/dt² - 4y = e³t with initial conditions y(0) = -5 and y(0) = 20.

11. Consider the function ƒ(t) = 0 if t < 2, = (t -2)² if 2 ≤ t ≤ 4, = 4 if 4 < t, Sketch the graph of ƒ(t). Write ƒ(t) in terms of Heaviside functions. Find the Laplace transform of ƒ(t).

12. Determine the value of k.

13. Find P (X < 1, Y > 3/2 ).

14. Find E(X), where (X, Y) has joint density function ƒX,Y(x, y).

15. Let X be a random variable with expected value E(X) = 0 and variance V ar(X) = 1. Use the Delta Method to find a two-term approximation to E(Y) and a one-term approximation to V ar(Y), where Y = 1 / (X + 1).

16. Let X1, X2, ..., Xn be independent and identically distributed with PDF ƒX(x; θ) = 2θxe^−θx² if x > 0, otherwise 0, for some unknown θ > 0. Find and simplify an expression for the log-likelihood function ℓ(θ; x). Derive the maximum likelihood estimate ˆ θ of θ. Show that the estimate ˆ θ found is a maximum likelihood estimator.